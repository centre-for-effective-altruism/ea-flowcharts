/* import expect from 'expect';
import flowchartReducer from '../reducer';
import { fromJS } from 'immutable';

describe('flowchartReducer', () => {
  it('returns the initial state', () => {
    expect(flowchartReducer(undefined, {})).toEqual(fromJS({}));
  });
});
*/
